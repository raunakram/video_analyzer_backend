from typing import Generator


def get_db() -> Generator:
    """Placeholder database session generator.

    Replace this with a real DB session (SQLAlchemy, async engine, etc.)
    when integrating a persistent store.
    """
    yield
